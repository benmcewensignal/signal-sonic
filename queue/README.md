Each JSON file committed here runs the pipeline once (push-triggered; runs are serialised).
Fields: mode (backfill|metadata|mixscan|mixrescan|artists|radepth|genres|week|names), month_from, month_to, scenes (comma-separated, backfill only).
